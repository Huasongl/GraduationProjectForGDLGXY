package com.gdlgxy.internshipcommunity.util;import android.content.Context;import android.content.Intent;import android.net.Uri;import android.os.Build;import java.io.File;import androidx.core.content.FileProvider;public class ApplicationUtil {    //6.0需要动态申请权限    //7.0需要fileProvider创建Uri    //8.0需要申请【安装未知来源应用权限】    public static void installApk(String apkPath, Context context) {        Intent intent = new Intent();        File apkFile = new File(apkPath);        intent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);        Uri apkUri;        try {            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {                apkUri = FileProvider.getUriForFile(context, context.getPackageName() + ".fileprvider", apkFile);                intent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION);            } else {                apkUri = Uri.fromFile(apkFile);            }            intent.setDataAndType(apkUri, "application/vnd.android.package-archive");        } catch (Exception e) {            e.printStackTrace();        }        context.startActivity(intent);    }}